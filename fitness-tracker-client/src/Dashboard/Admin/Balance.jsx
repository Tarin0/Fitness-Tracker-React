import { Helmet } from "react-helmet-async";

const Balance = () => {
    return (
        <div>
            <Helmet>
                <title>Dashboard | Balance</title>
            </Helmet>
            <div>

            </div>
        </div>
    );
};

export default Balance;